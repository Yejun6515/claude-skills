# Python caches
__pycache__/
*.pyc

# local working data (generated at runtime by some skills)
values.json
*.tmp

# per-PC local paths — create on each machine, see _config/README.md
_config/local-paths.md

# agentmemory plugin skills — installed via plugin, not synced here
commit-context/
commit-history/
forget/
handoff/
recall/
recap/
remember/
session-history/

# local template backups (recoverable via git history)
*.OLD.xlsx
