# Python caches
__pycache__/
*.pyc

# local working data (generated at runtime by some skills)
values.json
*.tmp

# skills with their own separate git repo (nested, synced independently)
primetals-text-style/

# agentmemory plugin skills — installed via plugin, not synced here
commit-context/
commit-history/
forget/
handoff/
recall/
recap/
remember/
session-history/
